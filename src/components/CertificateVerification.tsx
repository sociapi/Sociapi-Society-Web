import{useEffect,useState,type FormEvent}from"react";import{useNavigate,useParams}from"react-router-dom";import{findCertificate,type Certificate}from"../data/certificates";import{Container}from"./ui";
type State={status:'idle'|'loading'|'valid'|'invalid';certificate?:Certificate;id?:string};
export default function CertificateVerification(){const{certificateId}=useParams(),navigate=useNavigate(),[value,setValue]=useState(certificateId||''),[state,setState]=useState<State>({status:'idle'});function verify(raw:string){const id=raw.trim().toUpperCase();if(!id){setState({status:'idle'});return}setState({status:'loading',id});window.setTimeout(()=>{const certificate=findCertificate(id);setState(certificate?{status:'valid',certificate,id}:{status:'invalid',id})},250)}useEffect(()=>{if(certificateId){setValue(certificateId);verify(certificateId)}},[certificateId]);function submit(e:FormEvent){e.preventDefault();const id=value.trim().toUpperCase();if(id){navigate(`/verify/${encodeURIComponent(id)}`)}else setState({status:'idle'})}return <div className="verify-page"><section className="verify-hero"><Container><p>OFFICIAL RECORDS / SOCIAPI SOCIETY</p><h1>Verify certificate</h1><span>Check whether a certificate was officially issued by Sociapi Society.</span></Container></section><section className="verify-body"><Container><div className="verify-card"><form onSubmit={submit}><label htmlFor="certificate-id">Certificate ID</label><div><input id="certificate-id" value={value} onChange={e=>setValue(e.target.value)} placeholder="e.g. SA-011" autoComplete="off"/><button type="submit">Verify Certificate</button></div><p>Enter the ID printed on the certificate. QR codes open this page with the ID already filled.</p></form>{state.status==='loading'&&<div className="verify-loading" role="status"><span/>Checking official records…</div>}{state.status==='invalid'&&<div className="verify-result invalid" role="alert"><b>Certificate not found</b><p>No matching record was found for <code>{state.id}</code>. Check the ID and try again.</p></div>}{state.status==='valid'&&state.certificate&&<div className="verify-result valid"><header><span aria-hidden="true">✓</span><div><b>Valid certificate</b><p>Official Sociapi Society record</p></div></header><dl><div><dt>Certificate holder</dt><dd>{state.certificate.name}</dd></div><div><dt>Course / event</dt><dd>{state.certificate.course}</dd></div><div><dt>Program</dt><dd>{state.certificate.program}</dd></div><div><dt>Issue date</dt><dd>{state.certificate.issueDate}</dd></div><div><dt>Certificate ID</dt><dd>{state.certificate.certificateId}</dd></div><div><dt>Status</dt><dd>{state.certificate.status}</dd></div></dl>{state.certificate.certificateFile&&<a href={state.certificate.certificateFile} target="_blank" rel="noreferrer">View issued certificate ↗</a>}</div>}</div><aside className="verify-note"><b>About verification</b><p>Each issued certificate has a unique ID. Verification uses Sociapi's existing certificate database; this page does not generate or alter records.</p><a href="mailto:sociapisociety@gmail.com?subject=Certificate support">Certificate support ↗</a></aside></Container></section></div>}
